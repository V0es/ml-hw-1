# Часть 1
В первой части был проведён разведочный анализ данных и построены некоторые визуализации.

## Простейший EDA и обработка признаков

### Задание 1.

Первичный анализ и базовая предобработка данных:
 - Анализ на наличие дубликатов с помощью `duplicated` - недостаточен, т.к. позволяет найти только **явные** дубликаты, но не покрывает ситуацию, когда объекты различаются только целевой переменной
 - Дашборд с помощью `ydata-profiling` позволил проанализировать типы признаков, их распределение, кол-во пропусков и др.
 - Предобработка признаков: удаление единиц измерения в колонках `mileage`, `engine`, `max_power`
 - Заполнение пропусков медианным значением, которое считается по *трейну*

### Задание 2.
Продолжение подготовки данных для обучения:
- Удаление объектов с одинаковым признаковым описанием

### Задание 3.
Здесь надо было привести значения признаков `mileage`, `engine` и `max_power` к `float`, а также удалить признак `torque`. Приведение к `float` было сделано на этапе подсчёта медианы, так что просто удаляем `torque`.

### Задание 4.
Приведение стобцов `engine` и `seats` к типу `int`

### Задание 5.
Начало анализа данных: были посчитаны основные статистики для датасета и на их основе сделаны выводы:
 - У столбцов `selling_price` и `km_driven` довольно большой разброс данных
 - У признаков `mileage` и `max_power` присутствуют аномалии в данных
 - У категориальных признаков `seller_type` и `transmittion` наблюдается асимметрия по количеству объектов в категориях


## Визуализации

### Задание 6.
Был построен `pairplot` по числовым признакам, где наблюдается:
- Зависимость между целевой переменной `selling_price` и признаками       `max_power`, `km_driven` и `year`. Зависимость с `max_power` напоминает линейную, а с `km_driven` и `year` больше похожа на квадратичную.
- Попарная зависимость между признаками `engine`, `max_power` и `mileage`.

### Задание 7.
Была построена тепловая карта корреляционной матрицы, на которой подтвердились некоторые гипотезы, выдвинутые в прошлом пункте, а также обнаружилась заметная корреляция признаков `seats` и `max_power`

Также были построены гистограммы распределения объектов по категориям для категориальных признаков, на которых наглядно видна асимметрия распределения

# Часть 2

### Задание 8.
Были подготовлены тренировочный и тестовый наборы: сделана копия исходного датафрейма и отделена целевая переменная

### Задание 9.
Была обучена классическая линейная регрессия, которая показала следующие метрики:
- Для трейна:
    - $R^2$ = 0.5922629595849875
    - $MSE$ = 116873067751.52162
- Для теста:
    - $R^2$ = 0.5941441219017134
    - $MSE$ = 233297548204.61063

### Задание 10.
Была проведена стандартизация признаков при помощи `StandardScaler`. При этом скейлер обучался только на тренировочной выборке, чтобы избежать даталика. Далее линейная модель была обучена заново и получены метрики:
- Для трейна:
    - $R^2$ = 0.5922629595849875
    - $MSE$ = 116873067751.52162
- Для теста:
    - $R^2$ = 0.5941441219017262
    - $MSE$ = 233297548204.6033
Видно, что стандартизация практически не дала результата

### Задание 11.
В результате исследования коэффициентов стандатризатора выяснилось, что самым информативным признаком является `km_driven`

### Задание 12.
Была обучена `Lasso` регрессия на нормализованных признаках и получены метрики:
- Для трейна:
    - $R^2$ = 0.5922629595497046
    - $MSE$ = 116873067761.63504
- Для теста:
    - $R^2$ = 0.5941429546560312
    - $MSE$ = 233298219170.76022

Также после анализа весов выяснилось, что $L1$ регуляризация не занулила ни один из признаков, что, скорее всего, связано с низким значением коэффициента $\alpha$

### Задание 13.
Сначала был использован `GridSearchCV` с 10-ю фолдами для подбора оптимального параметра $\alpha$ для `Lasso` регрессии.
У лучшей модели коэффициент регуляризации получился: $\alpha=9.96$

Метрики модели с оптимизированным параметром:
- Для трейна:
    - $R^2$ = 0.5922629560865244
    - $MSE$ = 116873068754.31528
- Для теста:
    - $R^2$ = 0.5941324773038581
    - $MSE$ = 233304241851.97675

При данном коэффициенте регуляризации веса также не занулились

---
Далее были подобраны оптимальные параметры для `ElasticNet` регрессии. Лучшей модели соответствуют гиперпараметры:
 - $\alpha = 0.06$
 - $L1\ ratio = 0.75$

Метрики `ElasticNet` регрессии с оптимизированными параметрами:
- Для трейна:
    - $R^2$ = 0.5921444043237081
    - $MSE$ = 116907050234.61832
- Для теста:
    - $R^2$ = 0.590844970094775
    - $MSE$ = 235193994872.625


# Часть 3
Далее для улучшения модели попробуем добавить категориальные фичи

### Задание 14.
Предобработаем столбец `name` - выделим из названия автомобиля только марку, как наиболее информативную часть.

### Задание 15.
Закодируем категориальные фичи и `seats` с помощью `OneHotEncoder`, удалив при этом один из столбцов, чтобы избежать мультиколлинеарности

### Задание 16.
Используем `GridSearchCV` с 10-ю фолдами для подбора оптимального параметра регуляризации $\alpha$ для `Ridge` регрессии. Оптимизировать будем по метрике $R^2$.

Метрики `Ridge` регрессии с оптимизированным параметром:
- Для трейна:
    - $R^2$ = 0.7664893383653983
    - $MSE$ = 66933108039.792915
- Для теста:
    - $R^2$ = 0.7738582228516592
    - $MSE$ = 129992751127.6917

Видно, что удалось значительно повысить качество модели

# Часть 4
### Задание 17.
Посчитаем бизнесовые метрики для каждой из наших моделей.
> Посчитаем долю прогнозов, отличающихся от реальных цен на эти авто не более чем на 10% (в одну или другую сторону)

Приведу таблицу с бизнес-метриками, а также с $R^2$

| Модель | Знчение бизнес-метрики | $R^2$ |
|----------|----------|-----------|
|LinearRegression| 0.368| 0.5975932872244466|
|LinearRegression + StandardScaler| 0.227| 0.5941441219017263|
|Lasso + StandardScaler| 0.227| 0.5941429546560313|
|Lasso + StandardScaler + GridSearch| 0.227| 0.5941324773038581|
|ElasticNet + StandardScaler + GridSearch| 0.222| 0.590844970094775|
|Ridge + StandardScaler + OneHotEncoder + GridSearch| 0.306| 0.7741049385037158|

# Часть 5
Реализовано `Streamlit` приложение, где наглядно показан EDA, а также можно проверить модель на своих данных: либо вручную ввести признаки, либо загрузить `*.csv` файл

Что не удалось?
- Корректно обработать значения `mileage` - они были в разных единицах измерения
- Корректно обработать значения `torque`
- Не удалось сразу увидеть, что у целевой переменной распределение не нормальное - можно было бы логарифмировать её и таким образом, возможно, качество модели повысилось бы